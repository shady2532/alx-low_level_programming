#include "main.h"
/**
 * more_numbers - prints the numbers
 *
 */
void more_numbers(void)
{
	int i;
	for (i = '0'; i <= '9'; i++)
	{
		_putchar(i);
	}
	for (i = '0'; i <= '4'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
